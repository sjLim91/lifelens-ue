# CLAUDE.md

이 저장소의 에이전트 규칙은 `AGENTS.md`에 있다. 먼저 그 파일을 읽고 따른다.

다겸의 로컬 PC(Cowork / Claude Code)에서 작업할 때는 추가로 `tasks/COWORK_LOCAL_BUILD.md`를 읽는다.
성준의 원격(Astra / Codex) 세션에서는 `tasks/TASK_0N_*.md` 지시서를 번호 순서로 따른다.
